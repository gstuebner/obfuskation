Auswertung der übergebenen Beispieldaten
=======================================

Die Datei enthält 120 Datensätze. Auffällig sind zwei Konten:

- Manfred Bruns (Personennummer 44912),
  erreichbar unter paul.jahn167@example.invalid und (094) 1131413,
  Konto DE36737037830811363493 bei ENFBDEUWVVT, Stand 6836,65.
- Karla Faust (Personennummer 48695),
  Konto DE64058369578340996234, Stand 10742,82.

Vorschlag für die Auswertung:

```python
import pandas as pd

df = pd.read_csv("stammdaten.csv", sep=";", encoding="utf-8")
auffaellig = df[df["Personennummer"].isin([44912, 48695])]
print(auffaellig[["Personennummer", "Nachname", "EMail"]])
```

Die Anschrift „Fichtenweg 95, 80261 Bernburg" kommt in beiden
Beständen vor und sollte vereinheitlicht werden.
